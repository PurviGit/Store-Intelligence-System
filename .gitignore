# SQLite database files — must not be committed; auto-created on first API start
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Backup files
*.bak
*.backup

# Python cache

**pycache**/
*.py[cod]

# Pytest

.pytest_cache/

# Environment files

.env
.env.*

# YOLO model weights (auto-downloaded on first run)
*.pt

# Challenge datasets/videos

*.mp4
*.avi
*.mov
*.mkv
*.zip

# Challenge dataset video files (must not commit per submission rules)
# Real CCTV clips stay on your local machine
*.mp4
*.avi
*.mov
*.mkv
*.zip

# Generated demo clips directory (synthetic, but large — keep out of repo)
data/demo_clips/

# Challenge-provided reference files (do not commit)
data/POS*sample*.csv
data/sample_events*.jsonl
data/Brigade*.csv

# Dashboard screenshots (keep in docs/ only)
*.png
